# How to set up project

Recommended tools:
- vscode

Setup instructions:
1. copy the entire `lua-template` and `turing-api-lua` directories into your project directory.
2. rename the `lua-template` directory if desired.
3. edit `lua-template/script.lua`.

Export project:
1. copy the script file (not the api file) into your beatmap directory.