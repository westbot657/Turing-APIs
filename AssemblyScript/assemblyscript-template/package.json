{
  "name": "asmscript-template",
  "version": "1.0.0",
  "description": "AssemblyScript WASM template",
  "scripts": {
    "asbuild:release": "asc assembly/index.ts --config asconfig.json --target release",
    "asbuild:debug": "asc assembly/index.ts --config asconfig.json --target debug",
    "build": "npm run asbuild:release"
  },
  "devDependencies": {
    "assemblyscript": "^0.27.0"
  }
}
