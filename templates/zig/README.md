# How to set up project and compile

Required dependencies:
- zig

Setup instructions:
1. copy the `zig-template` directory
2. open project in an IDE of your choice

Compile project:
1. run `zig build`