#api turing_api
#version 0.8.0

// :Global: is special case for top-level functions
// For other classes, :ClassName [opaque]: marks the class as opaque
// .  prefix denotes a variable, which currently are not fully implemented in every template.
// :: prefix denotes a static function for classes
// No prefix denotes an instance method except in top level where the prefix doesn't matter

:Global:
test_global(name: &str) -> void
global_2_test() -> i32
my_test(a: i8, b: i16) -> String

:MyClass [opaque] :
object_func(a: i16) -> void : _my_class_object_func

:ColorNote [opaque] :
-- this is line 1\nthis is line 2
set_position(v: Vec3) -> self : _color_note_set_position
set_orientation(q: Quat) -> self : _color_note_set_orientation
set_transform(m: Mat4) -> self : _color_note_set_transform
get_position() -> Vec3 : _color_note_get_position
get_orientation() -> Quat : _color_note_get_orientation
get_transform() -> Mat4 : _color_note_get_transform
clone() -> ColorNote : _color_note_clone

:Log:
-- used to log messages to the console
-- idk, this is a second line
::info(msg: &str) -> void : _log_info
-- logs `msg` to the console
::warn(msg: &str) -> void : _log_warn
-- logs `msg` to the console as a warning
::critical(msg: &str) -> void : _log_critical
-- logs `msg` to the console as an error
::debug(msg: &str) -> void : _log_debug
-- logs `msg` to the console when in debug mode
